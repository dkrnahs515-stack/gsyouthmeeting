/* 실적 통계 7개 인원 분류 확장 */
(function(){
  const CATS=[['infant','유아','fa-baby'],['elementary','초등학생','fa-child'],['middle','중등','fa-school'],['high','고등','fa-user-graduate'],['college','대학생','fa-graduation-cap'],['adult','성인','fa-person'],['leader','지도자','fa-chalkboard-user']];
  const YK=['infant','elementary','middle','high','college'];
  const $=s=>document.querySelector(s), n=v=>Number(v)||0, esc=v=>(window.escHtml?escHtml(v||''):String(v||'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m])));
  function norm(o={}){o={...o};CATS.forEach(([k])=>o[k]=n(o[k]));if(CATS.reduce((a,[k])=>a+n(o[k]),0)===0&&n(o.youth)>0)o.elementary=n(o.youth);o.youth=YK.reduce((a,k)=>a+n(o[k]),0);return o}
  function blank(){let o={};CATS.forEach(([k])=>o[k]=0);o.total=0;return o}
  function add(a,b){CATS.forEach(([k])=>a[k]+=n(b[k]));a.total=CATS.reduce((s,[k])=>s+a[k],0);return a}
  function itemNums(item){item=norm(item||{});let sub=blank(),has=false;(item.subs||[]).forEach(s=>{s=norm(s);if(CATS.some(([k])=>n(s[k])>0)){has=true;add(sub,s)}});return has?sub:add(blank(),item)}
  function chip(k,l,i,v,cls=''){return `<span class="stats-sum-chip ${cls}"><i class="fas ${i}"></i> ${l} <strong>${v}</strong>명</span>`}
  function catChips(o){return CATS.map(([k,l,i])=>chip(k,l,i,o[k]||0)).join('')+chip('total','합계','fa-users',o.total||0,'total-chip lg')}
  function css(){if($('#stats7-style'))return;let s=document.createElement('style');s.id='stats7-style';s.textContent=`.stats7-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:14px}.stats7-card{background:#fff;border:1.5px solid #e2e8f0;border-radius:16px;padding:15px 16px;box-shadow:0 1px 3px rgba(0,0,0,.06)}.stats7-card.total{background:#eef6ff;border-color:#bfdbfe}.stats7-icon{width:36px;height:36px;border-radius:11px;display:flex;align-items:center;justify-content:center;background:#f1f5f9;color:#1a4a8a;margin-bottom:8px}.stats7-label{font-size:12px;font-weight:800;color:#64748b}.stats7-num{font-size:26px;font-weight:900;color:#1e293b}.stats7-num span{font-size:13px;margin-left:2px}.stats7-sub{display:flex;gap:5px;flex-wrap:wrap;font-size:11px;color:#64748b;margin-top:5px}.stats-table.stats7 th,.stats-table.stats7 td{font-size:11px}.stats-table.stats7 th.cat,.stats-table.stats7 td.cat{min-width:46px;text-align:center}.stats7-scroll{overflow-x:auto}.stats7-team-chips{display:flex;gap:5px;flex-wrap:wrap}.stats7-notice{background:#fff7ed;border:1px solid #fed7aa;color:#9a3412;border-radius:12px;padding:10px 12px;font-size:12px;font-weight:700;margin:0 0 12px}@media(max-width:1000px){.stats7-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:640px){.stats7-grid{grid-template-columns:1fr}}`;document.head.appendChild(s)}
  function sumPeriod(members,type){let byMember={},sum=blank(),has=false;members.forEach((m,idx)=>{let arr=m[type]||[];if(arr.length){has=true;byMember[idx]=arr}arr.forEach(p=>add(sum,itemNums(p)))});return{byMember,sum,has}}
  function rowCells(o){return CATS.map(([k])=>`<td class="stats-num cat">${o[k]>0?o[k]:'-'}</td>`).join('')+`<td class="stats-num total ${o.total>0?'has-data':''}">${o.total>0?o.total+'명':'-'}</td>`}
  function table(byMember,members,label,month,year,badgeCls,sum,headBg,headColor,border){let rows='';members.forEach(m=>{let memberName=m.name||'(이름 미입력)',badge=`<span class="stats-badge ${badgeCls}">${month}월</span>`;(byMember[m._idx]||[]).forEach(p=>{let nums=itemNums(p),subs=(p.subs||[]).filter(s=>s.text||s.content).map(s=>{let sn=itemNums(s);return `<tr class="stats-sub-row"><td></td><td class="stats-sub-name"><i class="fas fa-turn-down" style="font-size:10px;color:var(--gray-400);margin-right:4px;"></i>${esc(s.text||s.content||'세부 항목')}</td><td>${s.date?esc(s.date):'-'}</td><td>${s.time?esc(s.time):'-'}</td><td>${s.place?esc(s.place):'-'}</td>${rowCells(sn)}</tr>`}).join('');rows+=`<tr class="stats-prog-row"><td>${badge}</td><td class="stats-prog-name"><strong>${esc(p.name)}</strong><br><span class="stats-member-tag">${esc(memberName)}</span></td><td>${p.dates?esc(p.dates):'-'}</td><td>${p.time?esc(p.time):'-'}</td><td>${p.place?esc(p.place):'-'}</td>${rowCells(nums)}</tr>${subs}`})});rows+=`<tr class="stats-period-subtotal ${badgeCls==='prev-badge'?'prev-subtotal':'next-subtotal'}"><td colspan="5" class="stats-subtotal-label">${month}월 소계</td>${rowCells(sum)}</tr>`;return `<div class="stats-period-table-wrap"><div class="stats-period-table-header" style="background:${headBg};color:${headColor};border-bottom:2px solid ${border};"><i class="fas fa-${badgeCls==='prev-badge'?'calendar-minus':'calendar-check'}"></i> ${label}<span class="stats-period-month">${month}월 (${year})</span></div><div class="stats-table-wrap stats7-scroll"><table class="stats-table stats7"><thead><tr><th style="width:72px;">구분</th><th style="min-width:120px;">프로그램명</th><th style="width:80px;">일시</th><th style="width:75px;">시간</th><th style="width:80px;">장소</th>${CATS.map(([k,l,i])=>`<th class="cat" title="${l}"><i class="fas ${i}"></i><br>${l}</th>`).join('')}<th class="cat"><i class="fas fa-users"></i><br>합계</th></tr></thead><tbody>${rows}</tbody></table></div></div>`}
  function emptyPanel(label,month,bg,color){return `<div class="stats-period-table-wrap"><div class="stats-period-table-header" style="background:${bg};color:${color};border-bottom:2px solid ${color}30;">${label}<span class="stats-period-month">${month}월</span></div><div style="padding:24px;text-align:center;color:var(--gray-400);font-size:13px;"><i class="fas fa-inbox" style="font-size:24px;margin-bottom:8px;display:block;"></i>입력된 프로그램 없음</div></div>`}
  function renderStats7(){css();let container=$('#stats-content');if(!container)return;let {prev,curr}=getMonthInfo(),period=$('#stats-period-label');if(period)period.textContent=`${prev.year}년 ${prev.month}월(전월) ~ ${curr.year}년 ${curr.month}월(당월) 실적`;let has=[0,1,2].some(i=>(appState.programs[i]||[]).some(m=>(m.prev&&m.prev.length)||(m.next&&m.next.length)));if(!has){container.innerHTML=`<div class="empty-state"><i class="fas fa-chart-pie"></i><p>입력 탭에서 팀별 프로그램 자료를 작성한 후 확인하세요.</p></div>`;return}let grandPrev=blank(),grandNext=blank(),sections=appState.teamOrder.map(teamIdx=>{let team=TEAMS[teamIdx],members=(appState.programs[teamIdx]||[]).map((m,i)=>({...m,_idx:i})),prevData=sumPeriod(members,'prev'),nextData=sumPeriod(members,'next'),teamSum=add(add(blank(),prevData.sum),nextData.sum);add(grandPrev,prevData.sum);add(grandNext,nextData.sum);if(!prevData.has&&!nextData.has)return `<div class="stats-team-block"><div class="stats-team-header" style="background:${team.color}15;border-left:4px solid ${team.color};"><span class="stats-team-name" style="color:${team.color}"><i class="fas fa-users"></i> ${esc(team.name)}</span><span style="font-size:12px;color:var(--gray-400);">입력된 프로그램 없음</span></div></div>`;let prevPanel=prevData.has?table(prevData.byMember,members,'전월 활동 보고',prev.month,prev.year,'prev-badge',prevData.sum,'var(--orange-lt)','var(--orange)','rgba(217,119,6,0.3)'):emptyPanel('전월 활동 보고',prev.month,'var(--orange-lt)','var(--orange)'),nextPanel=nextData.has?table(nextData.byMember,members,'당월 활동 계획',curr.month,curr.year,'next-badge',nextData.sum,'var(--green-lt)','var(--green)','rgba(46,138,87,0.3)'):emptyPanel('당월 활동 계획',curr.month,'var(--green-lt)','var(--green)');return `<div class="stats-team-block"><div class="stats-team-header" style="background:${team.color}15;border-left:4px solid ${team.color};"><span class="stats-team-name" style="color:${team.color}"><i class="fas fa-users"></i> ${esc(team.name)}</span><div class="stats7-team-chips">${catChips(teamSum)}</div></div><div class="stats-lr-wrap">${prevPanel}<div class="stats-lr-divider"></div>${nextPanel}</div></div>`});let grand=add(add(blank(),grandPrev),grandNext),summary=`<div class="stats7-notice"><i class="fas fa-circle-info"></i> 기존 청소년 데이터는 초등학생으로 임시 환산됩니다. 필요 시 항목 수정에서 세부 연령대를 조정하세요.</div><div class="stats7-grid">${CATS.map(([k,l,i])=>`<div class="stats7-card"><div class="stats7-icon"><i class="fas ${i}"></i></div><div class="stats7-label">총 ${l}</div><div class="stats7-num">${grand[k]}<span>명</span></div><div class="stats7-sub"><span>전월 ${grandPrev[k]}명</span><span>당월 ${grandNext[k]}명</span></div></div>`).join('')}<div class="stats7-card total"><div class="stats7-icon"><i class="fas fa-users"></i></div><div class="stats7-label">전체 합계</div><div class="stats7-num">${grand.total}<span>명</span></div><div class="stats7-sub"><span>전월 ${grandPrev.total}명</span><span>당월 ${grandNext.total}명</span></div></div></div><div class="stats-period-bar-row"><div class="stats-period-bar prev-bar"><span class="stats-period-bar-label"><i class="fas fa-calendar-minus"></i> 전월 활동 보고 <em>${prev.month}월</em></span><div class="stats-period-bar-chips">${catChips(grandPrev)}</div></div><div class="stats-period-bar next-bar"><span class="stats-period-bar-label"><i class="fas fa-calendar-check"></i> 당월 활동 계획 <em>${curr.month}월</em></span><div class="stats-period-bar-chips">${catChips(grandNext)}</div></div></div>`;container.innerHTML=summary+sections.join('')}
  function install(){window.renderStats=renderStats7;if($('#page-stats')?.classList.contains('active'))renderStats7()}
  document.addEventListener('DOMContentLoaded',()=>setTimeout(install,1900));window.addEventListener('load',()=>setTimeout(install,1900));setTimeout(install,2400);
})();